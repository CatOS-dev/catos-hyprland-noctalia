-- Profile-scoped user window and layer rules.
-- Catdot seeds this file once and does not manage later edits.

-- Example:
-- hl.window_rule({
--     name  = "float-example",
--     match = { class = "example" },
--     float = true,
-- })
