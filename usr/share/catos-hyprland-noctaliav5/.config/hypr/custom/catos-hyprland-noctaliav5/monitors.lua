-- Profile-scoped output/monitor settings.
-- Catdot seeds this file once and does not manage later edits.
-- Add output blocks here after checking names with `hyprctl monitors`.

hl.monitor({
    output   = "",
    mode     = "preferred",
    position = "auto",
    scale    = "auto",
})
